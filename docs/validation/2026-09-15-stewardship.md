# 对象归并与材料维护验收

日期：2026-09-15。代码版本：v1.4.0（用户已确认）。范围：REQ-3-1/3/4、REQ-10-3、REQ-5-8、REQ-9-5/7，关联双端读取和管理统计。

首轮为 v1.4.0 本地开发验收；本次用户授权后，以修复远程升级兼容的 v1.4.1 完成生产部署与只读复验，详见文末。未对真实公开条目执行归并、关联或撤下。

## 验证结果

| 检查 | 实际结果 | 边界 |
| --- | --- | --- |
| 全库回归 | 项目 `.venv` 中运行 `python -m pytest -q`：292 通过、1 跳过 | 跳过的是默认关闭的外部集成检查；不等于生产实采 |
| 本轮后端用例 | `tests/test_stewardship.py`：20 通过 | 新闻与持续关注归并、冲突选择、陈旧预览拒绝、保持旧编号、私密候选归组、撤销保留后续编辑 |
| 原子事务 | 执行实际远程事务保护 SQL：正常归并提交；提交前并发修改触发回滚 | 使用隔离 SQLite 执行生成的远程事务语句；未执行生产 Turso 写入 |
| 材料边界 | 同日重试、漏检、三日／七日阈值、延期升级、内容变化、恢复、当前权限与旧原文版本均通过 | 时间为模拟自然日；不能当作连续三／七天真实运行证据 |
| 公开变化 | 当前 scope 与旧 scope 分开；固定窗口续读，撤回遮蔽旧快照说明；材料移除产生独立事件 | 只记录功能启用后的事件；不伪造历史；客户端需轮询 |
| 前端构建 | TypeScript 与 Vite 生产构建通过，82 个模块 | 构建成功不代表已部署 |
| 浏览器 | 本地管理员归并、撤销、关系增删、材料复核；旧链接引导、公开关系展示、未审核候选私密归组不发布；1440／390／320 像素无横向溢出 | 临时密码和临时 SQLite；非生产账户，无真机／屏幕阅读器验收 |

浏览器复验入口为 `tests/browser/stewardship_server.py` 与 `tests/browser/stewardship.cjs`。从仓库根目录启动前者，后者设置 `READING_BASE_URL=http://127.0.0.1:18050`、夹具内测试密码及本机 Playwright／Chromium 路径。脚本拒绝向非 localhost 主机执行写入。截图和原始结果保存在忽略目录 `output/validation/v1.4.0/`，不将夹具库、运行密码或私密快照纳入公开内容。

验收发现并修复：材料移除后旧状态仍展示；延期到七日后不重新提醒；未审核材料被公开门槛阻止进入私密草稿；恢复目标时来源关联未恢复；表单连续操作的可访问名称不稳定。归并发布另同步既有来源统计、批次发布记录和已处理候选。

## 首轮本地验收后列出的上线核对项

1. 私密备份、六张增量表升级、生产版本和双端接口；保持 13 条动态／29 项持续关注，除非另获具体内容发布确认。
2. 日维护真实调用与连续自然日观察，检查受预算限制的材料能否续检；真实材料变更由维护者核对，不以网页指纹变化认定事实变化。
3. 真实客户端读取 `scope=workspace`、保存检查点并于次日续读；服务仅提供查询，不声称主动推送客户端通知。
4. 真实对象归并或关联需逐项提出依据、预览并获得确认；没有用孤立测试内容填充正式站。

## 部署准备补验（v1.4.1）

用户授权推送部署后，已将 v1.4.0 提交 b359370 快进推送至 GitHub main 和开发分支。生产全库 55 张表已保存到私密本地 SQLite，独立恢复完整性检查通过；部署前公开动态 13 条、持续关注 29 项、应用版本 v1.3.3。

首次远程升级在提交 SQL 前因缺少显式事务失败，没有建表或改写公开内容。按补丁规则递增至 v1.4.1，改用与既有升级器一致的服务端原子事务，并新增远程升级与幂等回归。完整回归 293 通过、1 默认跳过；前端构建和仓库检查通过。

修复后真实生产升级完成：新增六张 fieldtofit_steward_* 表；再次执行升级成功且状态不变；news/watch/charts 三个已发布集合的存储哈希在升级前后相同。没有执行真实条目的归并、关系修改、失效处理或内容发布。生产切站及接口验收结果见下节。

## 正式站上线结果

- 部署代码提交：`6b36d1b0680fe3b20ace01d25195d2beca849da1`，应用版本 v1.4.1；GitHub main 与开发分支已推送。随后仅补记本批生产验收状态，按发布指南保持同批版本，不改运行代码。
- Vercel 项目：kevinnova-projects/fieldtofit；部署 `dpl_54KZPzXTvwC3dsb1558TXWdGb5HD`，READY、production。
- 部署地址：https://fieldtofit-df2rz74lk-kevinnova-projects.vercel.app；正式别名：https://fieldtofit.top。
- 健康接口 200、v1.4.1；MCP 初始化服务版本 1.4.1。当前 `scope=workspace` 首次读取与 resume_cursor 续读通过，当前事件数为 0（没有制造归并／失效事件）；`scope=legacy` 仍正常。
- 当前对象状态接口可读；匿名维护管理接口为 401；使用此前授权的本地管理配置可读取 42 项内容、0 个材料复核事项、0 个已确认关系和 0 个维护动作。未检查材料不能据此称健康，0 事件不代表自动完成审核。
- 正式站 Chromium 验收通过：For you、For your AI、关于、社区、来源和管理入口；新增维护模块可见；1440／390／320px 无横向溢出，0 个 pageerror。390px 管理截图人工检查通过。仅登录和读取，没有点击采集、归并、关系确认、材料决定或发布按钮。
- 公开动态 13 条，修订 `4db894ecc77a2041a79f48b0b532c8b772015200f91e477b3ef34cc156f3a004`；持续关注 29 项，修订 `051b8a5cb494f250825352f35ffda8d10f8ea6947e6104969f825f83af65b3d5`，与部署前一致。

原始结果与截图在忽略目录 `output/operations/v1.4.0-production/`（同一发布批次目录，最终运行版本以结果中的 v1.4.1 为准）。备份和生产配置仅保留私密文件，不公开。此轮没有创建新标签或 GitHub Release。

仍待：真实内容关系逐项确认、真实连续自然日材料检查及长期断源恢复、日常客户端跨日读取。此次只读部署验收不替代这些运行证据。
