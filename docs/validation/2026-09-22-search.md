# 搜索可读页面与独立内容页验收

开发日期：2026-09-22；正式部署收尾：2026-09-23。v1.5.12 完成搜索功能开发，v1.5.13 修复发布配置并部署，对应 REQ-18-1 至 4。用户明确现有介绍等文案不改写，并授权部署正式站。初次上线时尚未登录或提交搜索平台；随后 Google 验证／提交及地图排查进展见文末。

## 实现范围

- 五个公开主入口输出初始核心 HTML；28 条动态和 32 项持续关注各有稳定 `/news/D-…`、`/watch/CW-…` 网址，正文、解读、版本表、日期和出处读取既有公开 API/MCP 使用的同一发布集合。没有新增或改写发布内容。
- 五份中英文介绍、四组中英文主标题从原组件抽到共享 JSON，逐字核对与修改前源码相同；关于页原 FIELD → FIT 保留。浏览器使用原阅读／交接／访问计数组件，独立页默认展开进一步阅读；现有长页和旧锚点继续可用。
- 逐页 title／description／canonical／OG 在初始响应与客户端同步。新分享、内容标题、目录和相关入口采用独立网址；参数不生成新的 canonical。未新增虚构的结构化评分、作者或日期。
- 真实 robots 和自动 sitemap；当前隔离发布集合包含 5 个主入口 + 60 个内容 URL。`lastmod` 只取公开内容发生变化的发布历史时间；导入、草稿、私密字段和无变化发布不刷新。无可靠修改时间的种子或静态入口省略该字段。
- 新页面、归并跳转、公开详情读取和 sitemap 不缓存；下架后新请求不返回原正文，无效 ID 为 404，读取失败为 503，私密接口继续认证。管理／个人／兼容资料入口、错误和非搜索 API 不索引；预览及非正式域名有 noindex，预览 robots 禁止抓取。
- 前端构建复制当前资源哈希对应的 HTML 外壳到 Python 打包目录；Vercel 非 API 页由 Flask 处理，静态资源由部署输出提供。Docker 构建路径同步适配共享文案。没有生产迁移、新表、第三方追踪脚本或自动站长提交。

## 本地开发验证

1. 完整隔离回归：363 项通过、1 项跳过，25.98 秒；SQLite 临时库，禁用 dotenv，不连接生产凭据。之后补充归并缓存、兼容入口 noindex 及可读取的索引排除规则，最终 36 项相关回归通过，2.95 秒。
2. `tests/test_search_pages.py` 对全部公开详情逐项核对标题、介绍、解读、版本表、来源和公开修订；65 个 sitemap URL 与发布集合对账且全部可读。草稿保存／预览不泄露，发布／修改／下架立即反映；归并只跳转到公开目标。包括无变化发布日期、真实 404／503、故障脱敏、HTML 转义、预览规则与私密入口。
3. 新详情路径沿用访问会话：动态与持续关注详情在同一会话内只计一次，不存在条目不入统计；既有累计持久化、并发、隐私及清理回归继续通过。
4. 浏览器最终复验：40 个断言通过、页面错误 0。实际 Chrome／Playwright 检查无 JavaScript 的五个主入口、动态／资料正文和版本表；桌面 1440×1000、手机 390×844、英文深色模式；详情分享复制、旧 hash 展开、前进后退元数据、SPA 404 后清除正文和 noindex、手机无横向溢出。本段测试访问仅在隔离站点；正式站复验另见下文。
5. 前端 TypeScript／Vite 构建通过，88 模块。共享文案直接与修改前源码解析比对，通过。最终仓库检查：101 份文档、810 个本地链接、168 个相对导入、18 主 REQ／114 子 REQ、0 错误；`git diff --check` 通过。
6. Vercel 本地生产目标构建通过。首次直接在共享目录构建因缺少 uv 失败，临时安装 uv 后又遇到本地测试配置引起的 Python 锁文件不匹配；随后按正式上传排除规则，在无凭据临时目录、Python 3.12 构建成功。检查函数 `filePathMap` 中的新 Python 模块、共享 JSON、模板、生成外壳及既有统计 SQL／内容文件，外壳引用的资源均存在于静态产物；动态路由正确转入 Flask。生成的共享目录依赖输出和锁文件已清除，项目运行依赖未更改。

原始浏览器结果、桌面／手机截图、构建日志及打包检查结果保存在忽略目录 `output/acceptance/search/`。浏览器脚本为 `tests/browser/search.cjs`，隔离服务复用可配置端口的 `tests/browser/visits_server.py`。

## 正式部署过程

- 2026-09-22 首次 v1.5.12 远端构建失败：原 `.vercelignore` 的 `scripts/` 同时排除了 `frontend/scripts/copy-search-shell.mjs`，未替换当时的 v1.5.11 正式站。v1.5.13 改为根目录限定的 `/scripts/`，随后远端构建成功并绑定正式域名。
- 首次 v1.5.13 线上核验发现根路径被 Vercel 静态 `index.html` 优先响应，未执行 Flask 的 308 跳转；该检查失败不能记为验收通过。浏览器复验随后因 `ERR_NETWORK_IO_SUSPENDED` 中断，也未计为通过。
- 2026-09-23 继续同一未提交的 v1.5.13 批次，在 Vercel 显式配置 `/`、`/index.html` 永久跳转到 `/for-you`。远端构建成功，部署 `dpl_DeNaTMaxTxipJGC7C6yZ3wzWrmWm` Ready 并绑定 `https://fieldtofit.top`。已直接确认根路径返回 308 且保留查询参数；搜索相关隔离测试再次运行，14 项通过，2.65 秒。
- 跨日公开 API 比对发现三条动态的 `maintenance.materials` 与 `maintenance.status_revision` 改变；这些是维护状态，正文与公开内容修订未变。验收分别保留完整快照，并对除此字段之外的全量公开响应进行相等核对，没有用部署覆盖线上维护更新。

## 最终正式验收（2026-09-23）

1. 正式 `/api/health` 为 `v1.5.13`，MCP initialize 为 `1.5.13`；Vercel 远端前端／Python 构建通过，最终部署 Ready，正式域名已绑定。
2. 真实 Turso 公开集合为 28 条动态、32 项持续关注。已发布内容、全部非维护字段及公开修订与部署前基线相等；D-26／D-27／D-28 的维护材料状态变化独立保存在证据中，没有新增、覆盖或撤下任何已发布正文。
3. sitemap XML 内容类型、no-store 与 robots 地图声明正确。5 个主入口和 60 个详情共 65 个网址逐一返回 200，标题、描述、canonical、OG、可索引状态、可见介绍与公开修订均通过核对。
4. 不存在的动态／资料／未知页面返回 404 和 noindex；管理、个人清单与兼容参数入口保留 noindex。根路径、index.html 和带查询参数根路径均返回 308，跳到 For you 并保留参数。下架／归并的写操作只在隔离库验证，没有为测试修改正式内容。
5. 正式浏览器复验 40 个断言通过、页面错误 0；包括禁用 JavaScript 的主入口与详情、版本表及来源、桌面与手机、英文深色模式、分享、旧 hash、前进后退元数据和在浏览器局部模拟的 API 404 清除旧正文。已目视核对桌面与手机截图。
6. 原始部署前累计为 1；9 月 23 日复验前后均为 2，统计起始日一直为 `2026-09-22T01:52:43Z`，采集保持 active。没有重置数据库或补造访问；HTTP 验证使用监测 UA／DNT，浏览器自动化由现有 webdriver 规则排除。

最终仓库检查通过：101 份文档、811 个本地链接、168 个相对导入、18 主 REQ／114 子 REQ、0 错误；版本元数据与 `git diff --check` 通过。

最终部署、HTTP、浏览器日志与截图、前后公开快照及 JSON 结果保存在忽略目录 `output/operations/v1.5.13-release/`；核心结果为 `production-result.json`、`result.json`、`verify-final.log`、`browser-final.log`、`deploy-final.log` 和 `inspect-final.log`。

## 待执行与限制

- 从本机请求独立 Vercel 部署域名发生连接超时，未完成该域名的线上预览访问保护／noindex 实测；预览规则已在隔离测试通过。正式自定义域名的上述验收不受影响。
- 本机没有 Docker，未实际构建容器；已修改的多阶段路径与共享文案拷贝仍需容器环境验证。
- 无脚本响应提供核心静态阅读内容；图表交互、资料下载／复制、主题和语言切换等继续依赖浏览器脚本。首期中文公开正文没有虚构英文独立地址。
- 初次上线时 REQ-18-5 平台验证／提交尚未执行；Google 后续提交进展见文末。REQ-18-6 收录、曝光和点击仍待验证，没有自动监控；技术实现不代表已被搜索引擎收录。
- 开发依据：Google [JavaScript SEO](https://developers.google.com/search/docs/crawling-indexing/javascript/javascript-seo-basics) 与[站点地图](https://developers.google.com/search/docs/crawling-indexing/sitemaps/build-sitemap)官方说明；以本站实测结果区分实现、上线和收录。


## Google 提交与地图提速（2026-09-23，v1.5.15）

- 所有者已在阿里云添加 Google TXT 验证记录；阿里云权威 DNS 与 Google 公共 DNS 均返回一致记录。所有者随后提供 Search Console 地图提交截图和实际网址测试结果；地图报告显示「无法抓取／0 个网页」，实际网址测试显示可编入索引。所有者另报告已请求编入索引，不把该请求等同于收录完成。
- 本机两次抓取原地图均为 200、合法 XML、65 个公开网址，耗时约 8.62／8.68 秒；继续排查时复测约 8.67 秒。正式 robots 允许抓取并声明地图，无 noindex 响应头。最近可见的 13 次地图请求均为 200，日志未提供可确认 Google 身份的依据，不能据此声称 Google 已成功读取。
- Vercel 自定义防火墙规则为空，已生效的 WAF 配置为空；完整 overview 因套餐不含 IP Bypass 返回 402，未升级套餐或修改防护。浏览器控制持续超时，无法直接查看账号内错误详情。
- 原实现为地图加载两次完整公开详情及维护装饰，每次需多次远程往返。新实现使用同一只读事务获取公开集合、别名和真实更新时间，仍复用原公开校验与投影；不读取与地图无关的材料健康／关系字段，不增加缓存。
- 本批首先在独立工作副本开发。期间另一批内容以 `5139850`／v1.5.14 正式提交，新增五条动态；本批快进合入该提交并改为 v1.5.15，当前基线为 33 条动态、32 项持续关注、共 70 个地图网址。未改写这些内容或执行数据库发布。
- worktree 缺失 Vercel 自动识别所需的 GitHub 元数据，首次部署为 BLOCKED；使用完整本地克隆保留真实仓库与作者信息后，平台正常验证并构建。初次完整副本部署在取消请求前已完成；随后发布含最新 v1.5.14 源码的 v1.5.15，以完整保留两批更新。

本批实际验证：

1. 搜索页面与统一检索共 34 项相关测试通过；合入 v1.5.14 内容后再次通过，5.87 秒。覆盖公开范围、草稿／下架、归并链、真实日期、错误脱敏、远程只读批处理，以及维护状态读取故障不影响地图。
2. 本地前端构建通过（88 模块）；最终 v1.5.15 远端前端与 Python 构建通过。部署 `dpl_5kmgEURmakUxdcZRHCNLbWsYn3SE` Ready 并绑定正式域名，健康接口与 MCP initialize 均为 v1.5.15。
3. 新版三次地图读取均为 200，耗时 2.668／2.339／2.111 秒；此前 8.62／8.68／8.67 秒，合入最新内容后的旧路径基线 9.113 秒。三次 XML 与最新 70 个网址的发布前基线逐字相同，类型正确、no-store、没有 noindex；两套公开 API 全量响应未变。
4. 正式运行日志中三个新版地图请求均只包含一次 Turso HTTP 请求，证实远程事务路径生效。首页、D-26、CW-M10、错误状态与 robots 抽查通过；本批没有界面改动，不重复把此前双端测试记为本批新增测试。
5. 累计访问数复验前后均为 5，统计起始日仍为 `2026-09-22T01:52:43Z`，未重置数据库或制造访问量。

最终仓库检查通过：102 份文档、815 个本地链接、168 个相对导入、18 主 REQ／114 子 REQ、0 错误；版本同步与 `git diff --check` 通过。

证据已归档到项目忽略目录 `output/operations/sitemap-fetch/`。Google 地图读取成功与实际收录仍须平台回执单独验收；性能改善不能作为 Google 已成功处理的证据。
