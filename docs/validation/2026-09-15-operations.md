# 来源统计、问题待办与真实客户端验收

日期：2026-09-15；时区：Asia/Shanghai。源码交付 v1.3.2；真实客户端为 codex-cli 0.154.0-alpha.6.2（沿用账户默认模型）；实际部署状态见下方，未完成的多日验收不记通过。

| 范围 | 实际结果 | 限制 |
| --- | --- | --- |
| 后台来源阶段与问题队列 | 全套回归 272 项通过，1 项实时 Turso/MiniMax 联调按配置跳过；隔离业务测试通过：历史未知、重复计数、提交复核、源变更失效、发布边界、等待时间、延期与恢复 | 阶段数据从升级启用起记录，旧记录不补造 |
| 浏览器管理界面 | 桌面 1440×1000、手机 390×844；数量下钻、问题延期、打开对应草稿通过；0 浏览器异常、手机无横向溢出 | 使用隔离数据库和临时密码，未操作生产草稿 |
| Codex 实际线上读取 | 独立空目录、忽略个人工具配置、仅临时 FieldToFit MCP；D-12 查询与引文、CW-S04 interview-me 全文 14,313 字符分 5 段读到 has_more=false；D-13 正确认出仅链接、无实测 | 不代表执行技能或覆盖所有客户端 |
| Codex 故障恢复 | 本机故障代理注入初始化 HTTP 401、工具调用超时；真实客户端明确报错且未补造答案；解除后相同两个入口均取回 D-12，集合修订一致 | 这是隔离故障，不是人为破坏生产后恢复；模型连接重试不算 MCP 故障 |
| 日报安排 | 单个本地安排 ACTIVE，07:30 / 08:00；独立规则解析核对接下来三天均为这两个时间 | 尚未到首个真实触发，不能写准时交付通过 |
| arXiv 实查 | 两个官方检索主机返回 429；官方 cs.AI Atom 返回 200，634 条订阅项 | 634 是当次订阅规模，不是本项目收录数量；摘要补充不代表检索恢复 |
| 本机新代码逐源实采 | Meta 被公网地址校验拦截；arXiv 直连请求超时 | 与独立 HTTP 订阅可访问是不同网络路径；未关闭公网校验或代理凭据保护，生产结果另记 |
| 连续三日 / 跨日客户端 / 周维护 | 待真实自然日与维护日 | 不用同日重复测试代替 |

真实客户端基线：新闻集合 `4db894ecc77a2041a79f48b0b532c8b772015200f91e477b3ef34cc156f3a004`，持续关注集合 `051b8a5cb494f250825352f35ffda8d10f8ea6947e6104969f825f83af65b3d5`。interview-me 版本 `be4e44a9fbc5e8df0beaefadbb28bd22ee61cc39`，材料修订 `f245ae0e36920bf619af9e79b301ff7e8f3751371f05a3f40216a2fbab3b7b36`；offset 0、3000、6000、9000、12000，最后 2313 字符。原始客户端事件、最终回答、私密来源状态和截图在忽略目录 `output/operations/p0-operations-2026-09-15/`，不将后台材料和密码放入公开仓库。

## 发布核对

正式站 https://fieldtofit.top 已运行 v1.3.2，MCP initialize 报告 1.3.2。最终部署 `dpl_6h7rNnLq2koiaRt3y5bpTMLBPAs6`，地址 https://fieldtofit-hljyf96jk-kevinnova-projects.vercel.app。管理员登录、来源数量明细、问题面板及原批次模块正常；匿名统计读取 HTTP 401；手机无横向溢出、浏览器异常 0。

迁移前已保存私密备份，幂等新增两张表。阶段动作自 2026-09-15 12:28 北京时间开始记录，当日不算完整记录日。生产面板当前正式日流程包含 55 个启用来源、另有本地整理一行；当天已检查数包含升级前记录，不能冒充 55 个来源已在本版完整实采验收。

本次仅手动实采（不计自然日自动验收）：

| 来源 | 真实获取与队列 | 未完成项 |
| --- | --- | --- |
| Meta 官方公告 | 两次各获取 4 篇正文，重复条目去重后为 6 篇；最初把目录 page=2 从文章队列分出并读取；最终已有文章从 17 降到 15，page=3 保持待处理，错误列表为空 | 15 篇文章与 1 个目录页待续；未宣称全部历史覆盖完成 |
| arXiv 相关研究 | 官方 cs.AI 摘要补充取得 8 条摘要；源日期未知不以订阅/抓取日期补齐 | 原检索 HTTP 429，分页位置保留；补充渠道不代表检索恢复或已读全文 |

本版新增阶段统计最终记录 12 个新发现、6 个取得全文的候选、8 个取得摘要的候选；整理/发布为 0，与实际仅采集操作一致。采集前后三个公开集合逐项一致。人工重试不改变日采集周期，也不等于发布。

本地唯一安排 `fieldtofit` ACTIVE，规则解析出的 9 月 16–18 日每天都是 07:30 和 08:00。三日运行及隔日客户端观察从 9 月 16 日开始，9 月 22 日进行满周复查；截至本记录均未到达验收日期。次日先核对实际客户端基线与消息交付，缺记录如实保留待验。
