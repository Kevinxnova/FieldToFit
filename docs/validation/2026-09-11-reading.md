# FieldToFit 阅读结构与首发动态验收

2026-09-11 · 当前应用版本 1.0.0。对应 REQ-Y-01/.02、Y-02/.01/.02、新增 Y-04 全部四项，以及 F-03、F-05、AI-03 的本轮子集。

## 实现

近期动态内含 5 条速览与 10 条发布更新，DeepSeek-V4.1-Flash 排首；完整解读只在动态内逐点展开。资源区改名资源档案，详细操作折叠。新增桌面/手机目录、阅读高亮、锚点分享、自动展开和焦点移动；目录和筛选/分页同步。

新增版本化新闻文件与 GET /api/v1/platform/news、curated_news。两端返回相同修订，新增精选 MCP 后共 11 项；旧端点保留 9 项兼容工具（共 20）。新闻以允许公开的结构化字段提供，草稿/撤回不公开；原始新闻只提供来源链接，不冒充已存全文。

## 验证

- 隔离 SQLite 完整回归：178 passed，1 skipped。
- 新增新闻测试覆盖双端一致、DeepSeek 未知日期、逐点来源、草稿隔离、撤回、修订变化、私有备注、非法链接、覆盖声明和访问控制。
- 前端 TypeScript/Vite 构建通过：74 个模块。
- 浏览器：1440×1000 桌面和 390×844 手机通过；25 项隔离验收档案覆盖两页、搜索/空结果/清除筛选，另验 10 条动态/5 条速览、目录、锚点刷新、自动展开与焦点、Escape、无横向溢出和新闻失败状态。脚本见 [reading.cjs](../../tests/browser/reading.cjs)，验收档案不写入生产。
- 公网部署成功：`dpl_2jAHJFpgeSC1ygoLf8KNigzoDVmq`，正式域名 [fieldtofit.top](https://fieldtofit.top/for-you)。2026-09-11 03:38 UTC 实测 10 条动态、27 项原有档案、11 项精选工具；MCP initialize/list/curated_news 调用通过，动态内容和修订与 HTTP 完全一致。桌面/手机锚点、自动展开和 For your AI 入口通过；未将隔离 QA 数据写入生产。
- 动态公开修订：`172aa7d951c2eb711c9fb816cab4556bbfc4a1cc605a44fa5ed8a01076b0f55f`；前端构建 `index-Cvu_0Bmw.js` / `index-Dmy1QmgE.css`。
- 首次公网浏览器检查早于域名完成切换而超时；确认部署 READY、域名切换和新 API 200 后重跑通过。

## 边界

本轮新闻随 Git 审核和部署发布，未声称每日自动抓取这些新闻。后台 AI 新闻整理、全文采集、新闻历史正文与统一组织/产品关系仍待开发。资源使用原数据库已审集合，未将所有新候选自动入库。来源两张跟踪表、3 个真实日周期及日常 AI 客户端验收仍待完成；账户关闭，案例 pending。
