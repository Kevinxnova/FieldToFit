# v1.1.0 统一内容管理验收

2026-09-12。本文件记录本轮真实结果，不替代全项目或连续运营验收。

| 检查 | 结果 |
| --- | --- |
| 全量 Python 回归 | 221 passed，1 skipped；隔离 SQLite，未访问生产数据 |
| 新管理生命周期 | 13 项集成测试，覆盖迁移完全一致与幂等、选中/保存/预览不发布、Web/MCP 同源、权限、材料和草稿冲突、下架及恢复、日期分页、投稿与图表、每日原文任务、公开字段白名单和关闭自动初始化时的显式迁移 |
| 前端构建 | TypeScript / Vite 通过；管理界面独立按需加载 |
| 浏览器交互 | 1440px 实际登录、候选→编辑→保存→双端预览→明确发布通过；For you API 从 404 变为 200；旧路由跳转通过；390px 日审/图表编辑无水平溢出；深色编辑器可读；浏览器无脚本错误 |
| 生产迁移与部署 | 生产原样迁移已完成：38 个管理项、三个集合逐字段与迁移前相同；真实管理登录、四模块、图表预览、退出及关于页 v1.1.0 通过。真实草稿保存及原样图表发布复验通过，公开正文/图点/来源日期保持不变。 |

隔离浏览器站点使用临时数据库和临时测试密码，不向正式站点发布测试材料。现有公共资料和权限边界继续保持。仍未证明连续三个真实日周期、多编辑者实名审计、自动全文覆盖或真实投稿运营完成。

生产前置：只读完整快照包含 39 张既有表、13,668 条 tools 与 27 条旧资料记录；私密保存，独立 SQLite `integrity_check=ok`。首次生产检查发现自动初始化关闭，显式迁移已补齐原子建表，增加对应回归测试；没有为了修复而开启每次请求自动初始化。

跨区域写入：实际生产草稿保存暴露 Turso 五秒交互事务超时。现改为先读取与校验，再在单次服务端事务中重核全部已读取行集并写入；竞态返回 409，不自动重试不确定提交。新增真实 SQL 执行测试，覆盖原子写入、草稿/集合/来源竞态与下架恢复；生产草稿保存 3.45 秒成功；原样图表发布及结果读取 8.61 秒完成，确认三个公共集合无内容变化。


## 正式部署与数据边界

- 最终部署：`dpl_FvVWSUm9qJD26oCAsAKofT8nYmK3`，READY / production；绑定 [fieldtofit.top](https://fieldtofit.top)。[Vercel 部署记录](https://vercel.com/kevinnova-projects/fieldtofit/FvVWSUm9qJD26oCAsAKofT8nYmK3)。
- 生产后台已实际登录并浏览每日审阅、内容库、反馈、来源与运行；图表双端私密预览、退出及关于页 `v1.1.0` 通过，无浏览器脚本错误。
- 生产迁入三集合、38 个管理项：10 条动态、27 个持续关注主体、1 套 AA/Arena。迁移前后及原样发布后三份公开集合逐字段一致，190 个图点及原始来源日期保留。
- `curated_news` / `curated_watch` 与对应公开集合完全一致；管理员匿名访问 401，非法来源写入 403；管理数据与备份 `Cache-Control: private, no-store`。
- 当前日审可读取旧 tools、新资料、日维护原文任务，共 13,726 个待审候选（验收时刻计数，并非全是今天新增）。日期/来源/状态可筛选分页。
- 私密草稿保存使用既有 `CW-M01` 原文不变；原样发布使用既有图表且保留源日期。没有在生产新建测试候选或向公开站点增加示例内容。
- 全库迁移前快照和迁移后管理备份保存在被 Git 忽略的 `output/backups/`，权限为 600，未提交公开仓库。单项恢复已隔离实测；真实全库灾难恢复仍未宣称完成。

发布源码标签为 `fieldtofit-v1.1.0`；对应本轮一个交付提交。修复和部署迭代均在该批次内完成，没有改写历史标签。
