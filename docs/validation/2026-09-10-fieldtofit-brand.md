# FieldToFit v1.0.0 品牌替换验收

2026-09-10。按用户提供的名称、Field / To / Fit 定义和品牌素材执行。

| 项目 | 结果 |
| --- | --- |
| 本地项目 | `/Users/kevin/fieldtofit`；Git 关联更新为 Kevinxnova/FieldToFit |
| 网页品牌 | 标题、导航、页脚、分享信息、MCP 身份和下载名称均使用 FieldToFit |
| 关于页 | 先展示 FIELD → FIT、用户提供的定位与三段定义，再保留原双端使用与维护介绍 |
| 图片 | 采用用户素材的深浅色横排 Logo、SVG favicon、触屏图标及分享图；修复单服务品牌静态路径 |
| 运行版本 | 前后端及健康接口 1.0.0，关于页显示 v1.0.0 |
| 页面验收 | 本地关于页、浅色标志、390px 深色标志和分段布局实际检查通过 |
| 回归 | 167 passed、1 skipped；前端构建通过；文档检查 18 个 REQ、72 个子项、42 个历史锚点无错误 |
| 兼容 | 旧环境变量、SQLite 文件和浏览器收藏仍可读取；旧协议标识与历史材料保留 |
| 部署 | 新建 Vercel 项目已更名 fieldtofit，配置指向 fieldtofit.top；DNS 与生产绑定已生效，公网健康、品牌资源、27 项对象与 MCP 工具列表通过 |

品牌版本号不替代完整运行验收。关于更新机制、首批内容和 MCP 的未完成项仍保留于 [REQ list](../product/requirements.md)。

## 生产读取修复

公开索引改为单次查询固定成员、变化日志扫描固定上界、请求内批量读取修订与权限。游标独立持久化，后续请求重新检查撤回；审核发布的业务事务保留。HTTPS 连接在请求内复用，不自动重放结果不明的写入。20 项资料读取在开发机到生产库实测从约 80 秒降至 6.56 秒，公网本次约 3 秒；不作为持续性能保证。生产维护写入与连续真实日验收仍未完成。

## 2026-09-11 公网补验

生产部署 `dpl_8SfeznoPW8EGbboioNtZCFoqNWjx` 已就绪并绑定 https://fieldtofit.top。独立 GitHub runner 验证品牌静态资源、健康版本、27 项已发布对象、MCP 初始化与全部 10 项公开工具实际调用成功，覆盖检索、档案、原文、导出、公开历史、变化、来源、概览列表/单期及含正文资料包。[公开验收运行](https://github.com/Kevinxnova/FieldToFit/actions/runs/34500952498)。

最终完整回归 167 passed、1 skipped；前端构建及文档检查通过。上述为匿名 HTTP/MCP 协议验收，不替代日常 AI 客户端用户体验、后台生产维护写入和连续 3 个真实日周期验收。
