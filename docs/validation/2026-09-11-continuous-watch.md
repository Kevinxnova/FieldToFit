# FieldToFit 持续关注 CW1 验收

2026-09-11 · 软件版本保持 v1.0.0。用户已确认 CW1 内容并授权更新正式网站。

## 内容与实现

- 主展示为 27 个持续关注主体：模型族 9、工具 4、Agent 7、Skill 集合 3、Harness 4；10 条近期动态保持。
- 每项包含简介、结构化表格、两点 FieldToFit 解读、官方来源、核验时间及关联链接。模型分支、发布日期精度和仓库 Star 快照保持来源口径。
- 五类目录、搜索全文、类型筛选、深链接自动展开与焦点、手机目录、单项/合集 JSON 下载、交给 AI 均已实现。
- GET /api/v1/platform/watch 与 curated_watch 读取同一份允许公开的内容，返回相同修订；精选 MCP 12 项，兼容入口总计 21 项。
- 原有数据库档案、原文与历史保留在“历史资料与原文库”；新增内容采用 backend/knowledge/content/watch.json 审核发布，不修改生产数据库。

## 本地验证

- 隔离 SQLite 回归：188 passed，1 skipped。
- 新增用例验证 27 项 / 五类数量、网页与 MCP 完全一致、正文搜索、草稿/私有字段隔离、撤回及关联失效处理、修订冲突、读取令牌、非法来源和表格格式。
- TypeScript / Vite 构建通过（75 modules）。
- 浏览器在 1440×1000 与 390×844 通过：目录、深链接刷新/焦点、搜索/空结果/清除/类型、表格无页面溢出、AI 预览/下载、503 不显示虚假零条。已查看桌面 Kimi 和手机 Harness 截图。
- 脚本：[continuous-watch.cjs](../../tests/browser/continuous-watch.cjs)。初次浏览器测试在最后一步使用可见导航选择器查找手机折叠菜单而超时；改为检查容器文本后通过，产品代码无需调整。

## 发布状态

- 正式部署 **READY**：`dpl_6JxddixiwfqcfNEnT7uw8ussrkaT`，实现提交 `1d65611`，已绑定 [fieldtofit.top](https://fieldtofit.top/for-you)。
- 公网复验通过：五类 27 主体、10 条动态、12 项精选 MCP；HTTP / MCP 内容及修订完全一致。
- 桌面与手机的搜索、筛选、深链接、下载和错误状态通过。首次公网脚本在错误状态检查中未等待另一块新闻异步加载而断言 0；增加加载等待后重跑通过，产品代码未变化。
- 公开修订：`0cd8226818734d2c40fa7f84654ad0519167ad6e9c15a9821a935ef695b5bd2a`；构建 `index-Dpntflex.js` / `index-Ccoj8d4I.css`。
- 机器记录：[公网验收结果](2026-09-11-continuous-watch-public.json)。截图保留在本地验收输出，不加入网站品牌目录。
- 未修改生产数据库、公开账户开关或原有每日调度。

## 边界

原文覆盖 link_only，中文整理不冒充完整原文或实际运行结果。首次发布日期、部分 SDK 版本及 Skill commit 未核项仍明确保留；不计算没有连续快照的 Star 增长。CW1 随审核后的代码发布更新，不声称接入自动逐项日维护。统一对象数据库关系、来源跟踪表、多日运营与日常 AI 客户端验收继续保留未完成状态。账户待开放，案例 pending。
