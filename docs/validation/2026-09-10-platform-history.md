# 公开修订历史与筛选网页补验

2026-09-10 · Unreleased。延续[来源与筛选开发](2026-09-10-platform-sources.md)，本轮解决其中的网页补验缺口并新增公开修订历史。应用版本仍为 `1.1.0-beta.2`，未创建新提交、远程发布或生产部署。

## REQ 结果

| REQ / 子项 | 本轮结果 | 保留的边界 |
| --- | --- | --- |
| REQ-Y-02.03、AI-01.03 | 来源和 UTC 发布日期筛选、空结果、首尾包含、刷新恢复、清除条件已补验 | 多来源/多页数据由自动测试覆盖；真实预览仍只有一个对象 |
| REQ-Y-02.04、Y-03.01 | 手机筛选/历史布局、跨修订打开与返回原筛选列表；修复详情跳转丢筛选问题 | 全面辅助技术、离线及目标用户验收尚待执行 |
| REQ-Y-03.01、AI-02.03 | 人读/AI 页增加公开历史；原文包带截至所选发布的历史及超限续读 | 对象关系、真实上游多版本材料仍未补齐 |
| REQ-AI-03.01/.02 | curated_history 及同源 HTTP，现有 19 项 MCP 工具 | 官方 SDK 已验，日常 AI 客户端带引用回答仍待验 |
| REQ-X-01.03/.04 | 旧变化读取保持兼容，复用快照缓存；REQ/README/指南和更新记录同步 | 不新增表、不更新生产库、没有自动扩大精选名单 |

## 自动测试

新增 `tests/test_platform_history.py` 6 项，覆盖：历史截止修订、私有备注排除、新发布不混入既有分页、撤回阻止旧游标与正文、再发布后的旧历史脱敏、23 条历史先导出 20 条并续读余下 3 条、游标范围/过期、HTTP/MCP 一致与读令牌、未发布对象不可读取。

完整测试：**154 passed, 1 skipped，3.59s**。外部服务调试未执行，不计为通过。前端 TypeScript 与 Vite 构建通过，71 个模块。

官方 Python MCP SDK **2.1.1** 实际完成 HTTP/stdio 的 19 项工具发现、历史单条分页、原文包历史一致性，以及原有原文哈希、限长续读、来源/日期筛选、变化和概览检查。报告：[双传输公开历史记录](2026-09-10-platform-history-mcp.json)。

## 网页真实流程

使用隔离预览 `http://127.0.0.1:18000`，数据库 `/tmp/metis-platform-s1-preview/metis.db`；没有读取 `.env`、复用 GitHub 凭据或连接生产库。内容仍为 Deep Agents 一个真实对象、一个概览；上游版本未知。

已实际完成：

- 展开来源筛选并选择“人工整理”；原生日历把起始日设为 `2026-09-09` 后无匹配资料，概览保持原来的 2026-09-09 期次。
- 起始/截止均设为 `2026-09-08`，刷新后保留日期与来源条件，显示 1 个匹配对象。日期严格以 Metis 发布日期 UTC 为准。
- 清除条件后 URL 返回 `/for-you`；For your AI 中选择来源后仍能选择同一对象。
- 修订 6 显示事件 **6、4、3**，分别为资料修订、待复核、首次精选；修订事件明确显示“别名”变化。
- 页面生成的 JSON 原文包同样包含事件 **6、4、3**，仍包含 **7,991** 正文字符。
- 打开旧修订 3 后只显示事件 **3**，不把之后的修订混入早期档案。
- 从带来源/首尾日期条件的列表进入详情，再打开旧修订并返回，URL 仍保留完整来源和日期条件。
- 390×844 手机配置下，筛选视图的 `window.innerWidth` 与 `scrollWidth` 均为 **390**；筛选控件和公开历史截图已检查，单列展示无横向溢出。

日期输入使用浏览器原生可访问控件赋值完成；该环境的 Playwright 日期 fill 未形成有效值，因此不把 fill 调用本身计为通过。上一轮因工具额度中断的完整来源/日期网页补验已在这里完成。

## 当前阶段与后续

S2 人读与 S3 AI 取材继续推进：对象、材料、公开修订历史已有可核对链路。S1 的可维护精选规模、逐份材料检查、有依据的关系，以及 S4 的正式部署/恢复、连续 3 个真实日周期和目标用户使用仍待完成。

下一轮优先把首批材料接到真实检查流程，并记录逐材料失败/恢复与失效通知；随后补对象关系及真实上游多版本。公开账户待开放，AI 应用案例 pending。

## 收尾检查

历史视图同样测得视口与页面宽度均为 390。临时设备尺寸已复原。仓库检查为 63 份文档、552 个本地链接、86 个相对导入、18 个父 REQ、72 个子 REQ、42 个历史锚点，0 错误；`git diff --check` 通过。
