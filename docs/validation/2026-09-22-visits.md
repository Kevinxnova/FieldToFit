# 全站累计访问次数：开发与正式部署验收

日期：2026-09-22。对应 REQ-17-7 及 REQ-17-1/2/5/6 首期范围，源码批次 v1.5.11。当前共享目录同时有 v1.5.10 内容发布工作，本功能移入独立工作目录和 `codex/public-visits` 分支验证，未复制内容发布、凭据或生产数据库。

## 已完成与范围

公共页脚展示同一全站累计次数和实际起始日；中英文、明暗主题、手机底部导航已检查。有效公开页面加载且可见时上报；30 分钟服务端会话去重，多标签页共享浏览器随机标识。公开只读总数不增加次数，起始日前的流量不补造。

新增四张独立统计表，累计值与短期标识分离；本地 SQLite 及 Turso 单次原子批次共用同一组语句。标识只存摘要，满 24 小时后按访问或每日维护清理；存在退出、DNT/GPC、管理标记、机器人和预览／本机排除，来源校验和双层限流。读取接口不返回标识。禁用浏览器存储时不计数，并修复原阅读界面保存主题／语言时的存储异常。

## 实际执行

- 首轮完整回归：347 项通过、1 项跳过，19.06 秒；在独立工作目录执行，禁用 dotenv，测试使用临时 SQLite。
- 增量迁移补充后针对性回归：36 项通过，涵盖访问计数、原子事务、每日维护及安全用例。
- 浏览器：37 个断言通过，0 个页面错误。使用隔离 Flask 服务与临时 SQLite，模拟正常浏览器；多标签页、重复打开、DNT/GPC、管理页、完全禁用存储、机器人、不可见标签页、读取／上报失败、有限重试同一事件 ID，以及桌面／手机和英文深色显示均有实际检查。
- SQLite 验证重复建表和初始化不清零，清理标识不减总数；并发请求与中途事务失败可复现。Turso 使用真实 Hrana 批次结构在隔离 SQLite 中模拟执行，验证服务端条件回滚语义；当时没有连接真实 Turso；后续实际验证见下方正式部署。
- 最终完整回归：348 项通过、1 项跳过，19.51 秒；最终前端类型检查与构建通过，运行 v1.5.11 的隔离服务复验 37 个浏览器断言通过、0 个页面错误。
- `release_metadata.py --write` 同步 v1.5.11；`check_repository.py` 通过：99 份文档、800 个本地链接、152 个相对导入、18 主 REQ／114 子 REQ、42 历史锚点、91 旧子项映射、0 错误；`git diff --check` 通过。
- 使用已安装 Vercel build-utils 的实际 glob 函数核对部署包含规则，新 analytics.sql 与既有公开内容均匹配；这不是远程部署验收。

脚本为 `tests/test_analytics.py`、`tests/browser/visits_server.py`、`tests/browser/visits.cjs`。浏览器原始结果及截图保存在 `output/acceptance/visits/`（忽略目录）；图上的访问次数和日期来自隔离演示，不能当作正式站流量。

## 正式部署：2026-09-22

- 合入已发布 v1.5.10 的完整历史和内容；合并后完整回归 348 项通过、1 项跳过，21.41 秒；前端类型检查与构建通过（85 模块）。仓库检查：100 份文档、805 个本地链接、152 个相对导入、18 主 REQ／114 子 REQ、0 错误。
- 迁移前已生成生产数据库完整快照并本地恢复验证 integrity=ok。真实 Turso 使用临时表执行与采集相同的 SQL：首访、重复事件、会话内活动、另一浏览器及超时访问累计为 3；重复主键故障触发批次回滚。临时表随后删除，没有写入正式访客总数。
- 原子增量迁移创建四张独立统计表，迁移后正式累计表无数据。生产设启用开关及唯一来源 https://fieldtofit.top，其他凭据保持原样。数据库备份、环境文件及原始证据在忽略目录，未提交仓库。
- Vercel 正式部署 `dpl_4Si4UEFDJKJrinpRczserU6fS1nf` 为 Ready，别名已指向 https://fieldtofit.top；部署源码提交 `134a648`，后续只有本批发布状态收尾。正式 health 与 MCP initialize 均返回 v1.5.11。
- 正式 GET 总数返回 active、collection_enabled=true、正确来源和真实总数 0，started_at=null；第一笔真实有效访问才设起始日。DNT 排除请求返回 204；错误来源返回 403。28 条动态和 32 项持续关注的完整公开 JSON 与部署前逐字段一致。
- 正式桌面 1440×1000 与手机 390×844 页脚均显示真实总数，无横向溢出，手机计数不被底部导航遮挡；For you 公开内容请求正常。浏览器使用 DNT 及自动化标记，采集 POST=0，页面错误=0；不以自动验收填充正式流量。首轮脚本在 For you 内容尚未加载时过早检查 Laya 文本而失败，修正为等待公开内容响应后，完整复验通过。
- 原始记录位于 `output/operations/v1.5.11-release/`：backup-result.json、migration-result.json、production-result.json、inspect.log、deploy.log、desktop.png、mobile.png；该目录不入库。

## 未验证／未执行

未开通网站账号，未开发 UV/PV／内容曝光／复访后台，未执行 REQ-18 开发或搜索提交。正式真实总数在验收时仍为 0，不能称已有真实流量验证；后续真实访问与连续三个自然日仍待观察，不能用临时表模拟替代。过滤不保证识别全部机器人，存储清理和不同浏览器会影响估算。
