# FieldToFit v1.0.0 · 项目文件与公开素材整理

日期：2026-09-11。范围：用户已确认的文档与文件整理；具体首发内容提供提案，不发布网站内容，不更改应用逻辑。

## 已完成的整理

| 范围 | 结果 |
| --- | --- |
| 品牌 | 保留 5 份网站使用图片；删除完整品牌目录内 71 个文件、品牌压缩包及 public/brand 中 4 份未使用变体；README 复用网站 Logo |
| 版本 | README 中英文、文档首页、版本导航、ROADMAP 统一当前 FieldToFit v1.0.0；CHANGELOG 正文聚焦当前，旧记录移至 Metis 附录 |
| 历史 | 20 个既有历史文件迁入 docs/archive/metis/，含 beta.1/beta.2；修复相对引用，明确历史适用范围 |
| 需求 | P2 两条内容线、分点本期重点、跟踪主体/发现渠道表；保留 18 个 REQ、72 个子项与 42 个历史锚点；设计待实施、名单待确认分别标注 |
| 首发内容 | [具体候选](../product/launch-selection.md)：5 个主题、9 条动态、15 个档案候选；逐条来源、日期、文案、评价和限制，未入生产库 |
| 运行说明 | 修正当前 MCP 与实际日调度说明；保留被调用的兼容模块、数据库和配置，详见[目录清单](../architecture/repository.md) |

## 验证与边界

检查通过：70 份文档、523 个本地链接、89 个前端相对导入、18 个 REQ、72 个子项、42 个历史锚点，错误 0；前后端版本均为 1.0.0。`npm run build` 通过（72 个模块）；`git diff --check` 通过。5 份保留图片逐份与 HEAD 比较 SHA-256 相同；frontend/public 当前只有这 5 份图片。没有改应用代码，未新增对应功能测试，也没有将上一轮 167 项回归写成本轮重新运行。

本轮不修改数据库/凭据/定时配置，不创建标签、不重写 Git 历史、不发布新内容。完整素材包曾进入公开提交，本次清理只影响工作目录和后续提交，历史副本仍可能可获取。用户自己备份的宣传素材未访问或修改。
