# AI-03 / A-05 任务资料包补充交付

日期：2026-09-07。范围：用户指定优先补任务资料包，围绕一个具体任务完成事实、步骤、成功判据、真实结果；完成已授权的本地后台验收；先提出目录与版本展示方案。本轮未执行目录迁移或新版本发布。

## 按 REQ 展开的结果

| REQ / 验收点 | 本轮变化 | 验证及边界 |
| --- | --- | --- |
| AI-03：已有条件参与任务组织 | 已有环境参与平台、语言、部署、显存及 PDF 主题识别；扫描件、初学基础调整步骤；保留识别依据 | 改背景可以改变候选条件与行动顺序；规则识别只覆盖明确提示，不是任意长任务的全面理解 |
| AI-03：任务定义 | 可填写输入、输出、成功判据及输入类型；显式设置覆盖识别提示 | 缺失项保持“待补充”；用户要求和资源事实分开，不能把要求当作已经满足 |
| AI-03：四种采用路径 | 直接使用、基于开源扩展、自行实现、混合采用各有指令、依据、缺口和检查方式 | 无版本或有冲突的事实不进入有依据的采用路径；未知硬条件仍保留 |
| A-05：可运行完整案例 | pypdf 6.17.0 完成中文 PDF 金额提取、逐页包装和业务规则；支持文件输入与 JSON 输出 | 固定 CNY 行格式。独立实现的是业务规则，并非完整自研 PDF 解析器；不宣称修改过上游源码 |
| A-05：版本材料与成功判据 | 固定依赖、官方使用/许可/局限引用、三步复现、输出字段和边界判据 | 库能力来自文档；金额规则、包装层和成功判据属于示例设计 |
| A-05 / V-02：真实结果 | 五个合成 PDF、七项实际检查，保留输入哈希、页码、原文与版本；运行结果写入资源验证记录 | 金额 128.50 CNY、第 1 页；缺失、多金额、无文本和负数不猜测。空白页不是实际 OCR 测试 |
| AI-04：不适用与旧结果 | 扫描输入、资源版本变化、缺失依据或明确不满足的条件阻塞案例采用；最近失败不被旧通过覆盖 | 固定样本通过状态和当前任务适用性分别展示；用户文件尚未运行 |
| AI-01 / M-01 / H-04：人和 AI 共用资料包 | API / MCP 接收同样的 task_spec；页面导出使用这次请求返回的同一份 Markdown | 导出保留四条路径、事实来源、命令、预期、真实输出、环境和局限。MCP HTTP / stdio 均实际通过 |
| O-01 / H-01：后台界面验收 | 已授权登录；资料搜索、打开表单、摘要保存与恢复；来源配置回显与保存；处理队列；未配置模型报错；退出 | 仅本次临时数据库。没有执行真实模型生成、生产管理、归并或外部消息发送；不等于全部运营工作流已验收 |

## 证据

- 自动回归：**65 通过、1 跳过**。新增七项行为检查覆盖环境影响、事实冲突、四种路径、扫描件/版本/必需条件阻塞、文件产物、最新验证记录与 MCP 输入校验。跳过项仍为显式启用的外部 Turso / MiniMax 集成检查。
- 前端：TypeScript 与生产构建通过。浏览器实测案例填入、资料生成、自行实现/混合采用切换、扫描件阻塞提示、实际结果展示。窄屏布局检查通过。
- [新案例真实运行记录](../validation/2026-09-07-task-packet-results.json)：Python 3.13.9 / Darwin arm64，pypdf 6.17.0、reportlab 4.5.1。五组实际 PDF/JSON 文件在临时目录，不加入版本库；短运行输出加入 Git。
- [MCP 实际调用结果](../validation/2026-09-07-task-packet-mcp.json)：官方 Python MCP SDK 2.1.1，HTTP 与 stdio 均完成四类既有任务及新增完整资料包输入/输出检查。
- [用户可运行代码](../../examples/task_packets/pdf_amount.py)与[复现说明](../../examples/task_packets/pdf_amount.md)。完整案例已加入维护者审核的验证清单；清单现有九项，先前八项历史结果不被覆盖。

## 仍未完成的范围

这轮交付的是一个完整开发案例和可复用的任务资料结构，不能将 AI-03 / A-05 所有内容建设目标整体标成完成。复杂任务分解、任意背景的语义理解、完整自研工具、更多可复现案例与用户真实文件效果仍需继续建设。研究比较、学习路线和组会提纲也不由这个 PDF 例子替代。

上一轮审计指出的独立能力筛选、研究实验结果与导出、重复候选管理界面、处理时延统计、按任务条件持续关注、运行隔离和远程数据库多步事务等缺口仍在。详细状态以 REQ 表及其验证边界为准。

目录和展示的下一步见 [待对齐方案](2026-09-07-repository-and-release-plan.md)。README、CHANGELOG 和原有文件路径将在用户确认方案后再统一整理。
