# Metis 本轮交付与 REQ 验收表

日期：2026-09-07。基线：2026-09-06 goals-and-requirements v0.4，以及用户确认的 remaining-requirements-and-plan。当前 GPT 完成本次代码与首批资料整理；后续无人值守的在线生成仍需单独配置可用 API 凭证。

## 已得到的验证结果

- 自动检查：57 通过、1 跳过；最后一次资料包扩充后，导入回归检查另行通过。跳过项为需要显式启用的真实 Turso / MiniMax 集成检查，未访问生产数据库。
- 前端：TypeScript 与生产构建通过。浏览器实际核对任务查询、简报、英文切换与账户待开放页面；任务检索的 PDF 地址误召回已修正并有回归检查。
- 独立安装：新建 Python 环境，按 requirements 安装，导入 6 个有引用的样本；生产构建的 6 类直达页面返回 200；重新初始化后 6 条记录和 8 个运行检查保留；无模型密钥的任务查询可用。
- 实践：8 个受控最小例子实际执行通过。固定种子、数据划分、模型版本与输出见 [运行结果](../validation/2026-09-07-runtime-results.json)。另有 [4 份阅读/判断产物](../validation/2026-09-07-reading-cases.md)，尚未由真实目标用户验收，不能计为运行通过。
- MCP：官方 Python SDK 2.1.1，HTTP 与 stdio 各完成 4 类任务的检索、档案与证据读取，以及研究引用和简报读取；[协议结果](../validation/2026-09-07-mcp-results.json)。不是日常桌面客户端与自主 Agent 效果验收。
- 模型：两种 MiniMax 实际请求均限流；当前 GPT 已整理并导入 6 条双语样本。Chat Completions / Responses 两种协议及不完整响应处理通过模拟服务检查；未获得两个在线供应商的成功验收结果。
- 自动审批两次拒绝使用本次创建的临时密码登录本地管理页面，理由为缺少明确的后台登录授权。第二次前已核实进程只连接临时预览数据库。已停止此界面操作；管理表单实际登录后的浏览器验收仍待用户授权。此前独立测试数据上的管理 API 自动检查已通过。

## 逐项交付

“已实现”描述代码与当前可复核流程，不表示全部资料已整理，也不表示生产部署与真实需求已经验证。范围受限的项目明确列在最后一列。

| REQ | 需求 | 本次结果 | 验证边界 / 仍需工作 |
| --- | --- | --- | --- |
| D-01 | 来源管理 | 来源新增、编辑、启停、采集参数与手动重试 | 17 个默认来源；国内外团队与垂直覆盖仍可扩充 |
| D-02 | 持续采集与新鲜度 | 周期固定 1 天；分页、进度、失败恢复与处理阶段 | 分页恢复测试通过；连续 3 个真实日周期未观察完 |
| D-03 | 去重、归并与识别 | 稳定身份、重复候选、软归并、撤销拆分 | 自动相似度只出候选；跨来源归并仍需审核 |
| D-04 | 分类和标识 | 稳定 ID、主题、对象类型、任务能力标签 | 存量低质量与低相关资料仍需持续整理 |
| D-05 | 事实与证据 | 原文引用、版本核对、单位条件和未知状态 | 引用匹配不等于语义正确；需抽查模型提取 |
| D-06 | 关联及历史 | 关系编辑、双端变更游标、档案及材料历史 | 论文/实现/数据的内容关联覆盖仍不足 |
| D-07 | 人与 AI 一致 | 共用事实；冲突并列保留、管理解决、两端提示 | 自动检查冲突、纠错和下架一致性通过 |
| D-08 | 详细材料 | README、HTML、PDF 文本，章节和页码定位 | 扫描件、公式与表格结构不承诺完整解析 |
| I-01 | 信息筛选 | 主题、来源、类型、日期与关键词筛选 | 未知发表日不进入日期筛选 |
| I-02 | 分层事件详情 | 双语六项解读、重要性理由、引用与深读 | 首批 6 条整理；大部分存量仍为原始摘要 |
| I-03 | 双语重点简报 | 新知识库简报、历史日期与导出 | 只纳入已整理且发表日已知的材料；尚未持续自动运营 |
| I-04 | 持续跟踪 | 本地关注对象和主题的真实变化流 | 游标分页和已读；清除浏览器数据会丢失本地位置 |
| A-01 | 资源档案 | 多类型档案、条件、来源、版本、运行记录 | 预览 147 个资源，其中 1 个达到当前完整字段标准 |
| A-02 | 条件检索 | 任务主题与条件分离；数值、别名、分页、可选模型扩展 | 当前在线语义扩展未实际成功；基础检索已验 |
| A-03 | 采用门槛 | 部署、语言、平台、费用、显存、许可证等有依据字段 | 不把未记载视为不支持；多数资源仍有未知条件 |
| A-04 | 方案比较 | 2–4 项对比、条件核对、任务排序与热度分开 | 不把元数据相似视为全面胜出 |
| A-05 | 使用与开发材料 | 版本对应步骤、材料包、使用/扩展/自行设计路径与导出 | PDF 使用、包装扩展、规则抽取已跑；复杂集成仍需任务验证 |
| A-06 | 资源关系 | 替代、依赖、集成、实现等有依据关联 | 已有维护表单与接口；具体关系需要持续建设 |
| AI-01 | 独立查询 | 版本化 API，查询、详情、研究、任务、简报 | 基础服务不依赖生成模型凭证 |
| AI-02 | 按需深读 | 证据目录、分段读取、版本与覆盖范围 | 大文档按分页/分段读，不声称每份资料完整 |
| AI-03 | 任务组织 | 四类任务各自的产物要求、条件、材料与缺口 | 有来源的规则资料包可用；不是自动完成所有复杂任务 |
| AI-04 | 结论边界 | 满足、不满足、未知、版本冲突、范围内未找到 | 数值、负例、无模型回退与误召回测试通过 |
| AI-05 | 增量变化 | 单对象、对象集合、主题、游标边界与下架标记 | 同步的主题范围按当前分类计算；生产长周期待观察 |
| H-01 | 人类入口 | 全新信息/应用界面，任务、简报、研究和管理页面 | 公开页实际检查；管理页登录后操作被自动审批拦截 |
| H-02 | 独立分享 | 独立路由、元描述、稳定档案链接 | 本地生产构建直达通过；公网部署分享未验 |
| H-03 | 收藏与订阅 | 本地收藏与主题关注；账户灰色“待开放” | 按约定不开注册、登录、跨设备同步、个性化邮件 |
| H-04 | 来源导出 | 档案、对比、任务、简报、研究 Markdown 和 BibTeX | 保留来源、版本、未知条件与整理标识 |
| O-01 | 审核管理 | 事实/分类/状态表单、关系、归并撤销、分歧解决 | 管理 API 已测；界面操作待授权；Turso 原有连接无多语句事务，远程失败原子性未验 |
| O-02 | 运行状态 | 来源状态、处理队列、失败说明、重试与补采位置 | 已保存分页进度；长周期来源覆盖不能由单次测试推断 |
| O-03 | 自行部署 | 源码、生产静态页面、持久化目录、容器配置与日任务 | 全新本地依赖环境通过；本机没有 Docker，容器构建与生产 Turso 未验 |
| O-04 | 权限分离 | 公开读取、可选读令牌、独立管理凭证与来源校验 | 自动检查通过；不对浏览器返回服务器模型密钥 |
| O-05 | 需求反馈 | 需求/缺失/纠错/成功/失败/复用反馈与处理状态 | 尚无真实用户完成任务与再次使用的效果数据 |
| O-06 | 模型可替换 | 地址、模型、凭证变量、协议选择、故障回退及 GPT 批量导入 | 两协议模拟验收通过；真实在线服务成功切换待有效凭证 |
| M-01 | MCP 能力 | 9 个只读工具，HTTP / stdio | 官方 SDK 两协议工作流通过；日常客户端进一步验证 |
| M-02 | MCP 示例 | 接入页、四类任务提示与可重复客户端脚本 | 有实际协议调用产物；未宣称桌面客户端自主完成任务 |
| V-01 | 人工任务案例 | 8 个运行样本 + 4 份 GPT 阅读/判断产物 | 12 个场景均有产物，阅读产物尚待人工验收；同模型搜索基线对照未做 |
| V-02 | 自动验证 | 受维护者审核的脚本清单、版本匹配、超时、记录与变化后复验 | 8 个真实通过；不是通用 OS/网络沙箱，不执行网页提供的任意代码 |
| R-01 | 论文资料 | 摘要、方法/贡献/实验/局限、页码依据与引用 | 预览 101 篇；深度整理仍为少量样本 |
| R-02 | 研究资源关联 | 论文、实现、模型、数据、基准的有依据关系 | 明确区分已证实类型与仅相关链接；覆盖尚不足 |
| R-03 | 实验比较 | 数据、版本、划分、指标、流程、模型、硬件对齐表 | 条件不足明确不可直接比；新增一次同划分两方法真实实践 |
| R-04 | 学习与复现 | 阅读/实践结构、版本材料、前置知识与证据缺口 | 有 CPU 最小实践；不冒充整篇论文完整复现或学习成效 |
| R-05 | 专题资料 | 研究阅读清单、可选生成提纲、Markdown 与批量 BibTeX | 规则整理与导出可用；在线提纲生成及真实组会验收待完成 |

## 内容规模快照

本地预览数据库有 147 个资源、101 篇论文、88 条事件，6 条 GPT 双语整理，1 个达到当前完整字段标准的资源，2 个资源带 8 次本轮最小实践通过记录。首批 200 资源 / 100 研究条目 / 60 份完整任务资料包 / 12 场景是建设目标，不能把检索出的候选数或模板数算成已核验的资料包数。本轮没有达到 200 资源和 60 份完整任务资料包目标。

运行数据库和大段第三方材料不提交 Git。仓库包含可重复导入的短引用与 GPT 整理样本、受控实践代码、实际输出以及边界说明。注册保持关闭；没有进行生产数据库迁移或公网部署。

## 复验方法

1. 按 README 建立环境，前端运行 `npm ci` 和 `npm run build`。
2. 使用独立的 `METIS_DATA_DIR`，从仓库根目录运行 `python -m examples.editorial.load`，无需模型密钥即可导入 6 个样本；这是明确的示例导入，不是后台默认写入。
3. 安装 `examples/validation/requirements.txt`，运行 `python -m examples.validation.run_suite --output /tmp/metis-runtime-results.json`。脚本只执行清单内代码，并检查安装版本与档案是否一致。
4. 启动后端，安装 `mcp>=2.1.1,<3`，运行 `python -m examples.validation.mcp_client /tmp/metis-mcp-results.json`。可用 `METIS_MCP_URL` 指定本地地址。
5. 日常流程运行 `python -m backend.knowledge.daily`，每 1 天由调度器调用；各处理阶段保留结果。Vercel 增加每日 `/api/cron/knowledge` 有界处理；超出单次额度的积压不会被标成全部完成。
6. 新知识库生成模型配置使用 `METIS_MODEL_*`；GPT 示例为 Responses 协议。凭证仅在服务器环境配置，Codex 对话登录不被当作 API 凭证。旧策展功能的生成配置仍属于旧流程。

模型接口说明依据 [OpenAI Responses](https://developers.openai.com/api/docs/guides/latest-model)；Turso 验收边界参照其 [HTTP 连接与事务说明](https://docs.turso.tech/sdk/http/reference)。容器实际构建、生产数据库验收、3 天运行观察、真实用户案例和基线对照仍需完成，不能用本表中的代码实现替代。
